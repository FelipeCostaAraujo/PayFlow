import 'package:flutter/material.dart';

Widget errorInitialization() {
  return Center(
      child: Text(
    "Não foi possível inicializar o Firebase",
    textDirection: TextDirection.ltr,
  ));
}
